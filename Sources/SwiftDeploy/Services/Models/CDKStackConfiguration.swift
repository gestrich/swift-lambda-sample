import Foundation

/// Configuration constants for CDK stack
public enum CDKStackConfiguration {
    /// Default stack name used throughout the application
    public static let defaultStackName = "SwiftLambdaSampleStack"

    /// Default CDK directory relative to project root
    public static let defaultCDKDirectory = "cdk"
}
